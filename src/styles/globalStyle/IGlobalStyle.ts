export enum IDFlex {
  CENTER = "CENTER",
  LEFT = "LEFT",
  RIGHT = "RIGHT",
  SB = "SB",
}

export enum PositionPosEnum {
  XY = "XY",
  X = "X",
  Y = "Y",
}

export enum PositionTypePosEnum {
  AB = "AB",
  RL = "RL",
  FX = "FX",
}

export enum CursorType {
  POINT = "POINTER",
  NOT_ALWD = "NOT_ALLOWED",
}
